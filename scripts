// ==UserScript==
// @name        twitch points auto claimer
// @namespace   Violentmonkey Scripts
// @match       *://www.twitch.tv/*
// @grant       none
// @version     1.0
// @author      -
// @description 18.12.2025, 22:06:24
// ==/UserScript==
setTimeout(function() {
    console.log('trying to inject');
    injector();
}, 2000);

function injector() {
    setInterval(function() {
        var elem = document.getElementsByTagName('button');
        var clicked = false;
        for (var i = 0; i < elem.length; i++) {
            if (elem[i].ariaLabel == 'Получить бонус') {
                elem[i].click();
                console.log('%cclaim points button found, clicked, ' + new Date(), 'color:green');
                i = 999999999;
                clicked = true;
                break;
            }
        };
        if (!clicked) {
            console.log('%cclaim points button not found', 'color:red');
        }
        clicked = false;
    }, 90000);
    console.log('injected');
}

// ==UserScript==
// @name         youTube Video Speed
// @namespace
// @version      2025-12-16
// @description
// @author       You
// @match        *://www.youtube.com/*
// @icon
// @grant        none
// @run-at       document-idle
// ==/UserScript==
setTimeout(() => {
    console.log('setup');
    setInterval(() => {
        if (!document.getElementById('logo').hasAttribute('customspeed')) {
            document.getElementById('logo').setAttribute('customspeed', 1);
        }
        if (document.location.pathname == '/watch') {
            createDiv(0);
        } else if (document.location.pathname.includes('/shorts/')) {
            createDiv(1);
        } else {}
    }, 1000);
    console.log('done');

}, 1000);


function keyHandler(event) {
    let players = document.getElementsByClassName('video-stream html5-main-video');
    let playersStream = Array.from(players);
    if (event.keyCode == 107 || event.keyCode == 109) {
        playersStream.forEach((element) => {
            if (event.keyCode == 107) {
                element.playbackRate += 0.25;
            }
            if (event.keyCode == 109) {
                element.playbackRate -= 0.25;
            }
        });
        document.getElementById('logo').setAttribute('customspeed', players[0].playbackRate);
        updateDiv();
    }
    if (document.location.pathname.includes('shorts')) {
        if (event.keyCode == 74) {
            playersStream.forEach((element) => {
                element.currentTime -= 5;
            });
        } else if (event.keyCode == 76) {
            playersStream.forEach((element) => {
                element.currentTime += 5;
            });
        } else if (event.keyCode == 188) {
            playersStream.forEach((element) => {
                element.currentTime -= 0.01;
            });
        } else if (event.keyCode == 190) {
            playersStream.forEach((element) => {
                element.currentTime += 0.01;
            });
        }
    }
}

function createDiv(value) {
    let div = document.getElementById('mySpeed');
    if (div) {
        div.remove();
        document.removeEventListener('keydown', keyHandler);
    }
    div = document.createElement('div');
    div.id = 'mySpeed';
    div.style.color = 'orange';
    if (value == 0) {
        document.getElementsByClassName('style-scope ytd-watch-metadata').title.appendChild(div);
    } else if (value == 1) {
        document.getElementsByTagName('yt-shorts-video-title-view-model')[0].appendChild(div);
    } else {}
    document.addEventListener('keydown', keyHandler);
    updateDiv();
}

function updateDiv() {
    //let currentPage=document.location.pathname.includes('watch')?'watch':document.location.pathname.includes('shorts')?'shorts':'none';
    let curDiv = document.getElementById('mySpeed');
    let cv = parseFloat(document.getElementById('logo').getAttribute('customspeed'));
    let players = document.getElementsByClassName('video-stream html5-main-video');
    Array.from(players).forEach((element) => {
        element.playbackRate = cv;
    });
    curDiv.innerText = players[0].playbackRate + 'x <- videoSpeed. Use numpad\'s \'+\' to increase speed, \'-\' to decrease speed. Step is 0.25';
}
